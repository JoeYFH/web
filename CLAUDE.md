# 專案說明

## 用途
此儲存庫專門用來存放**靜態網頁**，提供個人製作的靜態網頁上傳與託管。

## 規則
- 每個網頁只能透過**指定的連結**才能開啟
- 網頁不對外公開索引，無法透過其他方式瀏覽或發現
- 每個靜態網頁放在各自的子目錄或以獨立檔案存放

## 目錄結構（預計）
```
web/
├── CLAUDE.md         # 本說明檔
└── [網頁名稱]/       # 各個靜態網頁資料夾
    └── index.html
```

## 注意事項
- 不建立全域導覽頁（index）以避免網頁被任意瀏覽
- 每個網頁須透過專屬連結才能存取

## Git 提交規範
- **所有的 commit 訊息必須使用中文撰寫**
- 每次上傳或更新都需附上清楚的中文註解說明本次變更內容
